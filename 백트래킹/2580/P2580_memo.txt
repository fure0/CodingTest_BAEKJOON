스도쿠

[입력]
아홉 줄에 걸쳐 한 줄에 9개씩 게임 시작 전 스도쿠판 각 줄에 쓰여 있는 숫자가 한 칸씩 띄워서 차례로 주어진다. 
스도쿠 판의 빈 칸의 경우에는 0이 주어진다. 스도쿠 판을 규칙대로 채울 수 없는 경우의 입력은 주어지지 않는다.

[출력]
모든 빈 칸이 채워진 스도쿠 판의 최종 모습을 아홉 줄에 걸쳐 한 줄에 9개씩 한 칸씩 띄워서 출력한다.

스도쿠 판을 채우는 방법이 여럿인 경우는 그 중 하나만을 출력한다.

[접근]
int[9][9] arr 선언
값을 입력 받는다

sudoku(int row, int col) 정의
    // 해당 행이 다 채워졌을 경우 다음 행의 첫 번째 열부터 시작
    // 행과 열이 모두 채워졌을 경우 출력 후 종료
    // 만약 해당 위치의 값이 0 이라면 1부터 9까지 중 가능한 수 탐색
        // i 값이 중복되지 않는지 검사
        possibility
    // 예상 값이 잘못 입력된 경우, 다시 입력을 받을 수 있게 0으로 돌린다.

sudoku(0, 0);


possibility(int row, int col, int value) 정의
    // 같은 행에 있는 원소들 중 겹치는 열 원소가 있는지 검사
    // 같은 열에 있는 원소들 중 겹치는 행 원소가 있는지 검사
    // 3*3 칸에 중복되는 원소가 있는지 검사

    return true; // 중복되는 것이 없을 경우 true 반환


[힌트]

몫에서 x3 이다 (몇번 나눌수 있는가)
System.out.println((0/3)*3); // 0
System.out.println((1/3)*3); // 0
System.out.println((2/3)*3); // 0
System.out.println((3/3)*3); // 3
System.out.println((4/3)*3); // 3
System.out.println((5/3)*3); // 3
System.out.println((6/3)*3); // 6